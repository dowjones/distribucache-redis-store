{
  "name": "distribucache-redis-store",
  "version": "4.0.0",
  "description": "Redis-backed Distribucache Store",
  "keywords": [
    "distribucache",
    "cache",
    "redis"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/areusjs/distribucache-redis-store.git"
  },
  "author": "nemtsov@gmail.com",
  "main": "./lib",
  "scripts": {
    "test": "sh -c 'npm run test-cover && istanbul check-coverage --statements 95 --functions 95 --branches 95 --lines 95'",
    "test-cover": "istanbul cover _mocha -- -r should --recursive",
    "test-watch": "mocha -r should --recursive -w -R min -t 20"
  },
  "dependencies": {
    "common-errors": "^0.4.18",
    "joi": "^6.0.8",
    "lodash": "^3.5.0",
    "redis": "^0.12.1",
    "redis-lockr": "0.0.1"
  },
  "devDependencies": {
    "istanbul": "^0.3.8",
    "mocha": "^2.2.1",
    "proxyquire": "^1.4.0",
    "require-directory": "^2.1.0",
    "should": "^5.2.0",
    "sinon": "^1.14.1"
  }
}
